## Issues for new-document-ex&editor-ex page
1. When the doc's content is too long, I can't see the toolbar, then I can't adjust the content's style, it should be a menu when I select a block of text.then I can adjust the style by clicking the menu.
2. Image can't be aligned to the left or right, it should be centered by default, I can adjust it by clicking the align icon in the toolbar.
3. I can't adjust cell size in the table, it should be adjustable by dragging the border between cells.
4. Table should have a float menu which allow me to add or delete rows or columns, merge cells. the table menu can be opened by clicking the drag handle.(please do not make the dnd feature diabled)
5. Table has a bug that reproduce step is as follow:
    1. click a table cell, make sure the cusor is behind the cell content.
    2. press down mouse left button and hold on, then move the mouse to the left, try to select the cell content.
    3. expect the cell content to be selected, but it is not. the visual effect is like I dragged the cell, when I release the mouse, new cells are created. the whole table is not looks the original one.this bug not happen every time.

   